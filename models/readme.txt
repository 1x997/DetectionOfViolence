模型类说明：
1. FPN.py为特征金字塔，与Faster RCNN配合使用（已完成）
2. FRCNN.py为Faster RCNN模型，用于第一部分的检测人体（未完成，建议跑官方代码）
3. SHDL.py为散网混合神经网络，用于第二部分的提取关节点，输出关节点信息（未完成，建议先读论文）
4. SVM.py为支持向量机，用于第三部分的动作类型判断（已完成，但缺少数据集）
5. Model.py为综合模型，应当在其他模型训练并测试完成以后使用（已完成，但暂时没什么卵用）

